name: 'server redirect'
description: 'redirect github action server ssh for debug'
inputs:
  user_passwd:
    required: true
    description: 'set ssh password'
  server_host:
    required: true
    description: 'server host for redirect part'
  time_limit:
    required: false
    default: 600
    description: 'service time limit'
outputs:
  context:
    description: 'result output'
runs:
  using: node12
  main: 'dist/index.js'
